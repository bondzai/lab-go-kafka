# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/

# Go build cache
# This cache can contain, among others, build outputs and compiled test binaries.
# It can be removed without adverse effects to the build process.
**/build
*.env
*.zip

# custom ignore
frontApp